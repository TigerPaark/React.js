// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;
// 이상한 식을하게되면 뜨는 말

// 2. String Type
let myName = "박세진";
let myLocation = "목동";
let introduce = myName + myLocation;

let introduceText = `${myName}은 ${myLocation}에 거주합니다`;

// 템플릿 리터럴 문법

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type(아무것도 없다)
let empty = null;

// 5. Undefined Type
let none;
console.log(none);

// undefined라는 값은 진짜 변수를 선언하고 이런 식으로 아무런 값도 할당하지
// 않았을 때 자동으로 들어가는 값
// null이라는 값은 우리가 이런 식으로 직접 명시적으로 변수에
// 할당해줘야하는 값이다.
